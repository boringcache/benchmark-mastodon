workspace = "boringcache/benchmark-mastodon"

# Managed BoringCache BuildKit benchmark. The managed backend is the only
# Docker cache surface, so there are no archive entries to declare here today.
#
# This file is the canonical config anchor; entries can be declared
# here if a hybrid local-archive contract is later added (e.g. a build
# context cache that the managed BuildKit surface cannot express).
